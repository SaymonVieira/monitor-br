services:
  - type: web
    name: monitor-br
    runtime: node
    buildCommand: npm install
    startCommand: node backend/server.js
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: TRANSPARENCIA_API_KEY
        sync: false
    plan: free
